# Copier en .env : `cp .env.example .env` — NE JAMAIS commiter .env !
PROXY_HOST=0.0.0.0
PROXY_PORT=443
PROXY_CERT_FILE=./certs/server.crt
PROXY_KEY_FILE=./certs/server.key
PROXY_RESPONSES_DIR=./responses
PROXY_LOG_LEVEL=INFO
PROXY_ENV=development
